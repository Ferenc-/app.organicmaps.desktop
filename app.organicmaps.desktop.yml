app-id: app.organicmaps.desktop
runtime: org.kde.Platform
runtime-version: '6.5'
sdk: org.kde.Sdk
command: OMaps
rename-desktop-file: OrganicMaps.desktop
rename-icon: organicmaps
finish-args:
  - --share=network
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --system-talk-name=org.freedesktop.GeoClue2.*
modules:
  - name: organicmaps
    buildsystem: cmake-ninja
    builddir: true
    config-opts:
      - -DCMAKE_BUILD_TYPE=Release
    cleanup:
      - '/lib' # static libraries and build configs
      - '/include'
    sources:
      - type: dir
        path: /home/ferenc/github.com/organicmaps/organicmaps
      - type: patch
        path: ./disable-world-feed-integration-test.patch
      - type: shell
        commands:
#         - 'bash configure.sh'
          - cp private_default.h private.h
